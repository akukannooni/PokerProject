using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DrawSystem : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void onClickAct()
        {

            /* Debug.Log(cards[51 - counter].name);
            GameObject tmp = GameObject.Find("cards[51 - counter].name");

            var counterMax = 52;
            // Debug.Log("test");
            if (counter < counterMax)
            {
                Debug.Log(cards[51 - counter].name);
                GameObject tmp = GameObject.Find("cards[51 - counter].name");
                Vector3 cardsVector = tmp.transform.position;
                cardsVector = this.gameObject.transform.position;
                cardsVector.x = -10 + 2 *counter;
                cardsVector.y = 0;
                cardsVector.z = -20;
                this.gameObject.transform.position = new Vector3(tmp.x - 2.0f,tmp.y,tmp.z - 20.0f);
                Debug.Log("test" + counter);            
                counter++;
            
            } 
            */
        }
}
